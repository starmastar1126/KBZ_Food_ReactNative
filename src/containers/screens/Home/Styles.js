import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    mainContainer: {
        flex: 1
    },
    titleContainer: {
        fontSize: 35,
        fontWeight: 'bold'
    },
    mobileContainer: {
        marginTop: 15,
        fontSize: 25,
    },
    emailContainer: {
        marginTop: 15,
        fontSize: 25,
    }
})

export default styles;